package com.mc.main.procedural;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

public class ConditionalsPracticeUnitTEST {
	
	@BeforeAll
	public static void init() {
		ConditionalsPractice.start();
	}
	
	// In this practice, it is better to practice with a task
	// this is because conditionals have important value with unit testing
	// 
	// When a method is tested, we look at performing line coverage as well
	// as method coverage. We are also checking to see that the method result
	// is what we might expect when passing in a particular value
	@Test
	public void test() {}

}
